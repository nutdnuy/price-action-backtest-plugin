# Backtest Setup

These commands are the intended V1 workflow and become runnable after the
CLI/runtime tasks are implemented.

Run the strict setup check:

```bash
python3 scripts/price_action_backtest.py setup-check --strict
```
